import type { Settings } from "./lib/types";

const settings: Settings = {
  host: "localhost",
  port: 3000,
  // basicAuth: {
  //   username: "admin",
  //   password: "password",
  // },
};

export default settings;
